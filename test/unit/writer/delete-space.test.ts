describe('writer/delete-space', () => {
  describe('verify()', () => {
    it.todo('rejects if the submitter is not authorized to delete');
    it.todo('rejects if the space is not found');
    it.todo('pass if the submitter is the space controller');
  });
});
