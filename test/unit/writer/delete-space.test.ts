describe('writer/delete-space', () => {
  describe('verify()', () => {
    it.todo('rejects if the user is not authorized to delete');
    it.todo('rejects if the space is not found');
    it.todo('pass if the user is the space controller');
  });
});
